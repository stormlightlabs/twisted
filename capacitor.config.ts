import type { CapacitorConfig } from '@capacitor/cli'

/** Capacitor packaging settings shared by future Android and iOS projects. */
const config: CapacitorConfig = { appId: 'io.ionic.starter', appName: 'twisted', webDir: 'dist' }

export default config
